[package]
name = "gpure-bundler"
version = "0.1.0"
edition = "2024"
description = "bundler library for the Gpure ecosystem"
license = "MIT"
authors = ["izra mulya putra guzman < izramulyaputra@gmail.com >"]

[dependencies]
