[package]
name = "gpure-types"
version = "0.1.0"
edition = "2024"
description = "implementation of all pillar types"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
serde_json = "1"